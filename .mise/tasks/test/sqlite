#!/usr/bin/env bash
#MISE description="Run SQLite backend tests (delegates to .github/scripts/nix.sh test sqlite)"
set -euo pipefail
source "${MISE_CONFIG_ROOT}/.mise/lib/common.sh"

print_status "Running SQLite tests"
exec bash "${MISE_CONFIG_ROOT}/.github/scripts/nix.sh" test sqlite "$@"
