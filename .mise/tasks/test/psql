#!/usr/bin/env bash
#MISE description="Run PostgreSQL backend tests, requires a running server (delegates to .github/scripts/nix.sh test psql)"
set -euo pipefail
source "${MISE_CONFIG_ROOT}/.mise/lib/common.sh"

print_status "Running PostgreSQL tests"
exec bash "${MISE_CONFIG_ROOT}/.github/scripts/nix.sh" test psql "$@"
